@model sistemaDeAdocaoParaAnimais.Models.Animal

@{
  Layout = "_LayoutDeslogado";
}

<section>
<div class="exibirLado">
        @{
            var animais = ViewBag.pets;
        }

        @{
            if (animais != null)
            {
                @foreach (var animal in animais)
                {
                    <div>
                        <div class="card" style="width: 18rem;">
                            <div class="petsss">
                                <img src="@animal.ImagensPet" class="card-img-top" alt="...">
                                <div class="card-body">
                                    <h4 class="card-text">@animal.Nome</h4>
                                    <p class="card-text">@animal.Descricao</p>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            }
        }
    </div>
</section>